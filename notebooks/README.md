# Notebooks

The production engine lives in `src/`. Use this folder for exploratory
notebooks only. The command below reproduces the project outputs without
depending on notebook state:

```bash
python -m src.pipeline
```

